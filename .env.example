BETTER_AUTH_SECRET=replace-with-a-long-random-string
BETTER_AUTH_URL=http://localhost:3000
DATABASE_URL=./data/app.db
PORT=3000
# Comma-separated user ids granted unconditional admin access by the
# better-auth admin plugin (in addition to anyone whose `role` is "admin").
BETTER_AUTH_ADMIN_USER_IDS=
